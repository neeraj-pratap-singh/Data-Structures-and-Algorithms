# Create an empty Python list and populate it with integers from 1 to 10 using a loop. Print the resulting array.
# Initialize an empty list
integerArray = []

# Populate the list with integers from 1 to 10
for i in range(1, 11):
    integerArray.append(i)

# Print the resulting array
print("Resulting array:", integerArray)
